name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (Maven + Java 21)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vitalmas-backend
    steps:
      - uses: actions/checkout@v4
      - name: Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Maven version
        run: mvn -v
      - name: Build & test
        run: mvn -B -DskipTests=false -DskipITs=true --no-transfer-progress clean verify

  frontend:
    name: Frontend (Vite, opcional)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Detect frontend presence
        id: detect
        run: |
          if [ -f vitalmas-frontend/package.json ]; then
            echo "has_frontend=true" >> $GITHUB_OUTPUT
          else
            echo "has_frontend=false" >> $GITHUB_OUTPUT
          fi

      - name: Node 20
        if: steps.detect.outputs.has_frontend == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: vitalmas-frontend/package-lock.json

      - name: Install deps
        if: steps.detect.outputs.has_frontend == 'true'
        working-directory: vitalmas-frontend
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build (skip if no script)
        if: steps.detect.outputs.has_frontend == 'true'
        working-directory: vitalmas-frontend
        run: |
          if npm run | grep -qE '(^| )build( |:)'; then
            npm run build
          else
            echo "No build script defined, skipping."
          fi
